import React from "react";

import { Typography } from "@mui/material";

const Oculto = (props) => {
    return (
        <div>
            <Typography>
                Ancho: {props.width}
            </Typography>

        </div>

    )
}

export default (Oculto);